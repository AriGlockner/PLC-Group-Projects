var x = 0;

try {
  x = 0;
  var y = 10;

  try {
     while (x <= y) {
       x = x + 1;
       if (x == y) {
         throw y;
       }
     }
  }
  catch (e2) {
     x = x + e2;
     return x;
     throw x;
  }
}
catch (e1) {
  x = x + e1;
}
return x;